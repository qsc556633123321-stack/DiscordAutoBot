# Community Roadmap Mutation Port Session Forecast

The future infrastructure adapter should use the existing per-invocation
Roadmap Resource Session. Edit uses retained `M`; Send uses ensured channel and
then retains exact `S`. The current production Pair is deliberately
lookup-only; no mutation surface was added here.
