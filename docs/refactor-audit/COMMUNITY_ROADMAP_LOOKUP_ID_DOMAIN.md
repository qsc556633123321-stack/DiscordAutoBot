# Roadmap Lookup ID Domain

Legacy JavaScript truthiness is the contract. `undefined`, `null`, empty string,
`0`, and `false` skip fetch. Normal, whitespace, malformed strings, numbers,
and objects are truthy and are supplied to one fetch without validation.
