# Lookup Message Identity Handoff Blockers

The current Port Result cannot carry Message identity, while legacy edit requires it. No production accessor exists. Runtime redirect, mutation redirect, second fetch, message resolver/cache/registry, retry, and normalization remain prohibited. This slice adds no production code.
