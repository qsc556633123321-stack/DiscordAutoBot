# Legacy Edit Contract

The exact retained Message receives one `edit(payload)`. On success the same
message remains the runtime return value and existing ID is persisted. Rejection
propagates unchanged and stops persistence/Roadmap continuation.
