# Community Channel Setup Implementation Forecast

The next implementation may add only a narrow Infrastructure compatibility
adapter and alter the corresponding Concierge helper calls. It must preserve:
exact cache matching, create specs/reasons, Guide-only parent repair,
Guide-only swallowed overwrite failure, no retries, no rollback, exact returned
channel identity, and current publication ordering.

Forbidden without another preparation: broad V3/bootstrap integration,
permission-template redesign, category/channel ordering changes, topic metadata,
logging changes, persistence changes, Guide/Roadmap/Welcome changes, role or
presentation changes, JSON schema changes, and a generic Application setup plan.
