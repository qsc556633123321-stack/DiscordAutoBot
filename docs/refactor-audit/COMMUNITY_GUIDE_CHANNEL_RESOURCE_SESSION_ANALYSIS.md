# Community Guide Channel Resource Session Analysis

An infrastructure-local session could retain the ensured channel and expose
lookup/edit/send capabilities internally. It must not become an Application
service locator or leak a Discord object. Its owner, lifetime, ensure boundary,
and composition implications require a separate preparation slice.
