# Guide Runtime Pair Missing-ID Compatibility

For a missing or falsy guide message ID, a future Pair Creation-only path
would create one Pair and make zero port calls. Legacy lookup remains zero and
legacy send remains one.
