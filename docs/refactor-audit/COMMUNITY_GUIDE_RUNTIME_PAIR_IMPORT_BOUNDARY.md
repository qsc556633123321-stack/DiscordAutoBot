# Guide Runtime Pair Import Boundary

A future runtime may depend only on the Composition Feature, not directly on
Session, lookup adapter, mutation adapter, or Pair Factory. This slice adds no
runtime import.
