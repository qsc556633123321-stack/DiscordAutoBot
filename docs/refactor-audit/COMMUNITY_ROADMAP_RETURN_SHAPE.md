# Roadmap Return Shape

`setupRoadmapPanel` returns `{ channel, message }`. Edit returns the fetched
message object; send returns the exact `channel.send` message object. Branch
details are not returned.
