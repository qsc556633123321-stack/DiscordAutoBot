# Message Identity Continuity Contract

For an available lookup, fetched Message `M === Session retained M === future handoff M === legacy edit receiver M`. Fetch count is one and edit count is one. Wrapping, cloning, resolving, or re-fetching `M` is prohibited.
