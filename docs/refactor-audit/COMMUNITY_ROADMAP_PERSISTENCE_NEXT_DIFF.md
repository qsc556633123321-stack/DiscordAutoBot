# Roadmap Persistence: Approved Next Diff Forecast

The next approved diff may add a small application-only Roadmap persistence
request/value object and pure mapper to `{ guildId, patch }`. It may add unit
tests and exports if required by local application conventions.

It must not modify `communityConcierge.js`, `saveOnboarding`, the filesystem
adapter, composition, formal JSON, Discord mutation paths, or deployment data.
