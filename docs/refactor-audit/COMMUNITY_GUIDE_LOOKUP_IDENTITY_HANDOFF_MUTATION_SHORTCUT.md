# Mutation Redirect Shortcut Rejection

Lookup redirect plus `mutationPort.edit` would change mutation ownership, error handoff, and future result mapping. This preparation keeps legacy `message.edit` and `channel.send`; mutation redirect is explicitly excluded.
