# Roadmap Resource Session Preparation Blockers

This slice provides a test-only candidate only. Production session wiring is
blocked until its isolated implementation and regression coverage are approved.
Guide session reuse and generic publication sessions are rejected.
