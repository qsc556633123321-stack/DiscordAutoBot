# Roadmap Lookup Message Handoff Problem

A future result cannot expose Discord Message objects through Application.
Runtime still needs exact fetched `M` to edit and return it. Roadmap-specific
Resource Session preparation is the approved next candidate; a second fetch
and raw Message application results are rejected.
