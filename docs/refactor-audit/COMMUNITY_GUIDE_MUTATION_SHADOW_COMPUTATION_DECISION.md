# Guide Mutation Shadow Computation Decision

Shadow computation is rejected. It would add runtime work without an approved
observation sink and could alter logging, timing, or error visibility. The
future target is not shadow mode: it is a narrowly reviewed Plan-controlled
edit/send branch, with legacy Discord calls and persistence still retained.
