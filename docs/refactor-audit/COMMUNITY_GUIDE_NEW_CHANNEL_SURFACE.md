# Guide New Channel Surface

The missing-channel path awaits `guild.channels.create` with `GuildText`,
correct parent, and onboarding overwrites. A create rejection aborts ensure;
a successful return is the created channel object used by legacy publication.
