# Community Guide Channel Ensure Boundary

`getOrCreateGuideChannel()` includes lookup/create, naming, category placement,
channel type, and permission behavior. Moving it to an Application Port or
resource session would have a broad observable mutation blast radius. It stays
legacy-owned in this preparation.
