# Community Roadmap Lookup Adapter Identity Boundary

The adapter returns only the application-safe result. It does not call or
expose `getRetainedMessage`. Exact Discord Message identity remains private to
the Resource Session and can be introduced later through a separately approved
Roadmap pair/runtime handoff boundary.
