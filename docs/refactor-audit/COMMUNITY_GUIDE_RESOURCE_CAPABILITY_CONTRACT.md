# Community Guide Resource Capability Contract

Concept only: an infrastructure-local `GuidePublicationResourceContext` might
provide `lookupTrackedMessage`, `editTrackedMessage`, and `sendMessage`. It is
not a new port, session, service locator, or production implementation in this
slice.
