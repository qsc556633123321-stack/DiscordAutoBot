# Guide Channel Test-Double Compatibility

Some historical tests use partial channel doubles that cannot model a
successful Discord ensure result. Constructor-surface tests use explicit
existing-like and created-like valid doubles; incompatible doubles are retained
only to characterize validation, not as production reachability evidence.
