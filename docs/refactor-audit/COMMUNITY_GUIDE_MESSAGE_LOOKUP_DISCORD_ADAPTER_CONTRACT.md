# Community Guide Message Lookup Discord Adapter Contract

Future candidate only:

```text
createGuidePublicationMessageLookupDiscordAdapter(dependencies)
  -> { lookup(request) }
```

It would implement `GuidePublicationMessageLookupPort`, but dependencies remain
unapproved because channel resolution is not yet characterized. This document
does not create production JavaScript or a runtime adapter.
