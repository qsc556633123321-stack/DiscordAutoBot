# Roadmap Persistence Request: Next Diff Forecast

The next approved diff may prepare a Roadmap-specific reuse adapter or feature
that accepts the implemented request, maps it to the existing generic input,
and delegates to the existing generic feature. It must not introduce a writer,
repository, schema migration, runtime redirect, or asynchronous behavior.
