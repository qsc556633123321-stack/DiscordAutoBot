# Guide Resource Session Skip Model

Force mode and missing/falsy tracked IDs skip message lookup entirely. The
session still exists only if Send needs the ensured channel. The Plan remains
Send and the message-fetch count remains zero. No session method can turn a
skip into a lookup.
