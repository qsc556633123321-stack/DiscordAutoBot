# Roadmap Session Input Contract

Candidate constructor: `createRoadmapPublicationResourceSession({ ensuredChannel })`.
It receives the exact already-ensured channel and must not resolve a guild,
client, channel ID, repository, or stored message identity.
