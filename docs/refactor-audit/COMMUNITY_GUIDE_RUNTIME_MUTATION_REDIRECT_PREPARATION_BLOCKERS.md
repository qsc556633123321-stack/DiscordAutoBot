# Runtime Mutation Redirect Preparation Blockers

Runtime mutation remains legacy-owned. Edit can reuse the retained lookup
Message, but adapter success returns only an ID and failure results do not retain
the raw rejected Error. Send returns a new Discord Message that Session/Pair do
not retain. Therefore Runtime Mutation Redirect Only is not approved.
