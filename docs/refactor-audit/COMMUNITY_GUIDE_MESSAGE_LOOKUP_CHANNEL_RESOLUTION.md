# Community Guide Message Lookup Channel Resolution

Legacy has an ensured guide channel before lookup. A scalar-ID adapter would
need cache/fetch/guild/client resolution, define missing/rejected/wrong-type
behavior, and may add one or more channel calls. None are legacy behavior.
There is no fallback channel and no approved extra lookup. This is the principal
blocker for production adapter implementation.
