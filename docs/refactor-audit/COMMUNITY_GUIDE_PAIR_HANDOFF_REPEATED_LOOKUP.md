# Community Guide Pair Handoff Repeated Lookup Invariant

A later successful lookup replaces the retained identity. A failed lookup clears it. The getter has no lookup side effect and therefore cannot increase fetch count.
