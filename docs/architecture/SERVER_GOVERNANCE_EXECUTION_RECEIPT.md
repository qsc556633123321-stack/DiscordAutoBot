# Server Governance Execution Receipt

Each completed attempt returns an immutable receipt containing transaction, guild, plan fingerprint, actor, timing, status, operation and rollback results, irreversible completed operations, final verification, audit reference, and receipt fingerprint. Runtime resource IDs are transaction-local and never alter the immutable approved plan.
