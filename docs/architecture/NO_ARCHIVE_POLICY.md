# No Archive Policy

Server Governance v1 never creates archive categories and never emits archive actions. A managed resource with replacement evidence may be planned as `SAFE_DELETE`; uncertain resources remain `REVIEW` or `REVIEW_DELETE`. Legacy archive commands remain outside this model until separately deprecated.
