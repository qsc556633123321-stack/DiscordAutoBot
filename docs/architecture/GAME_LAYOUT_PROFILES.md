# Game Layout Profiles

Games declare `layoutProfile` in the canonical game registry. `full` produces chat, LFG, info, and voice entry; `compact` produces chat/LFG and voice entry; `voice_only` produces voice entry. Governance v1 reads these profiles only; activity does not automatically reduce a game layout.
