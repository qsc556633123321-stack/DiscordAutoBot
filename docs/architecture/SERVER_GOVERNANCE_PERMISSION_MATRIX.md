# Server Governance Permission Matrix

The machine-readable release-candidate matrix is generated by
`buildGovernancePermissionMatrix`. It covers everyone, guest, member, game,
every `game:<id>`, interest roles, and administrative roles against every
canonical resource. It verifies that member/generic-game roles cannot view a
specific game, while only the matching game role can.
