const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const s=fs.readFileSync(path.resolve(__dirname,'../../src/systems/communityConcierge.js'),'utf8');assert.match(s,/lookupPort\.lookupTrackedMessage/);assert.doesNotMatch(s,/RoadmapPublicationResourceSession/);console.log('Roadmap resource session remains hidden behind the runtime Pair');
