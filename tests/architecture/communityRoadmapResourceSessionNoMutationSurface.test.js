const assert=require('node:assert/strict');const {createRoadmapPublicationResourceSession}=require('../../src/infrastructure/community/roadmapPublication/RoadmapPublicationResourceSession');const s=createRoadmapPublicationResourceSession({ensuredChannel:{id:'c',messages:{async fetch(){return null;}},async send(){return {id:'sent'};}}});assert.equal(typeof s.editTrackedMessage,'function');assert.equal(typeof s.sendMessage,'function');assert.equal(typeof s.getRetainedMutationFailure,'function');console.log('Roadmap session exposes only the approved mutation surface');
