const assert=require('node:assert/strict');const {createRoadmapPublicationResourceSession}=require('../../src/infrastructure/community/roadmapPublication/RoadmapPublicationResourceSession');const s=createRoadmapPublicationResourceSession({ensuredChannel:{id:'c',messages:{async fetch(){return null;}}}});assert.equal(typeof s.editTrackedMessage,'undefined');assert.equal(typeof s.sendMessage,'undefined');assert.equal(typeof s.getRetainedMutationFailure,'undefined');console.log('Roadmap session has no mutation surface');
