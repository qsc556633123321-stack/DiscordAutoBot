const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const s=fs.readFileSync(path.resolve(__dirname,'../../src/systems/communityConcierge.js'),'utf8');assert.equal(s.includes('message.edit(payload)'),true);assert.equal(s.includes('channel.send(payload)'),true);assert.equal(s.includes('mutationPort.edit'),false);console.log('Runtime mutation redirect remains preparation only');
