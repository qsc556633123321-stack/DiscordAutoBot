const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const s=fs.readFileSync(path.resolve(__dirname,'../../src/systems/communityConcierge.js'),'utf8');const guide=s.slice(s.indexOf('async function setupCommunityGuide'),s.indexOf('async function setupRoadmapPanel'));assert.equal(guide.includes('message.edit(payload)'),false);assert.equal(guide.includes('message = await channel.send(payload)'),false);assert.equal(guide.includes('mutationPort.edit'),true);assert.equal(guide.includes('mutationPort.send'),true);console.log('Runtime mutation redirect is active');
