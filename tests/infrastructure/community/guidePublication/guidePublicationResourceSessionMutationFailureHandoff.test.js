const assert=require('node:assert/strict');const {createGuidePublicationResourceSession}=require('../../../../src/infrastructure/community/guidePublication/GuidePublicationResourceSession');
(async()=>{let failure=undefined;const message={id:'m',async edit(){throw failure}};const session=createGuidePublicationResourceSession({ensuredChannel:{id:'c',messages:{async fetch(){return message}},async send(){throw failure}}});assert.deepEqual(session.getRetainedMutationFailure(),{hasFailure:false});await session.lookupTrackedMessage('m');for(const value of [new Error('x'),{},'x',1,null,undefined]){failure=value;await assert.rejects(session.editTrackedMessage({}),e=>e===value);const h=session.getRetainedMutationFailure();assert.equal(h.hasFailure,true);assert.strictEqual(h.failure,value)}console.log('Guide mutation failure Session handoff passed')})().catch(e=>{console.error(e);process.exitCode=1});
