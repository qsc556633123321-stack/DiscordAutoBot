const assert=require('node:assert/strict');const {createFakeCommunityRoadmapPublicationResourceSession}=require('../../fakes/community/FakeCommunityRoadmapPublicationResourceSession');
(async()=>{for(const raw of [new Error('x'),'x',1,{},null,undefined]){const s=createFakeCommunityRoadmapPublicationResourceSession({ensuredChannel:{id:'c',messages:{async fetch(){throw raw;}}}});assert.deepEqual(await s.lookupTrackedMessage('x'),{kind:'Unavailable'});assert.equal(s.getRetainedMessage(),null);}console.log('Roadmap session lookup rejection passed');})().catch(e=>{console.error(e);process.exitCode=1;});
